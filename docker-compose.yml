services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sigh-import-app
    ports:
      - "51423:51423"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
